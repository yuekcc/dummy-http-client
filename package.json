{
  "name": "dummy-http-client",
  "version": "1.0.0",
  "description": "a http client with API looks like HttpClient from angular",
  "main": "index.js",
  "module": "dist/index.js",
  "scripts": {
    "build": "npm run build:esm",
    "build:esm": "esbuild src/index.ts --platform=browser --format=esm --outdir=dist --minify  --sourcemap",
    "lint": "eslint"
  },
  "keywords": ["http client", "promise", "angular", "fetch"],
  "author": "",
  "license": "WTFPL",
  "devDependencies": {
    "@types/qs": "^6.9.4",
    "@typescript-eslint/eslint-plugin": "^4.1.0",
    "@typescript-eslint/parser": "^4.1.0",
    "esbuild": "^0.7.1",
    "eslint": "^7.8.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "qs": "^6.9.4"
  }
}
